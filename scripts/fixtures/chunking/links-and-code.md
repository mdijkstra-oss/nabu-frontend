# Loader configuration

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v0/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/00-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v-1/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v1/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/01-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v0/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v2/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/02-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v1/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v3/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/03-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v2/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v4/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/04-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v3/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v5/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/05-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v4/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v6/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/06-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v5/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v7/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/07-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v6/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v8/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/08-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v7/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v9/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/09-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v8/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v10/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/10-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v9/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v11/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/11-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v10/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v12/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/12-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v11/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v13/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/13-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v12/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v14/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/14-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v13/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v15/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/15-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v14/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v16/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/16-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v15/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v17/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/17-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v16/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v18/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/18-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v17/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v19/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/19-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v18/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v20/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/20-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v19/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v21/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/21-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v20/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v22/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/22-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v21/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v23/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/23-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v22/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v24/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/24-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v23/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v25/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/25-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v24/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v26/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/26-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v25/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v27/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/27-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v26/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v28/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/28-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v27/loader/legacy.html#fallback) and logs a warning through `logger.warn`.

Set `chunk.target` to a value the loader can hold; see [the loader reference](https://docs.example.com/v29/loader/reference?section=chunking&anchor=target) for the exact bounds. The default is `1000` characters and the override lives in `config/29-loader.toml`. If the value is below `min`, the loader falls back to [the previous release's behaviour](https://docs.example.com/v28/loader/legacy.html#fallback) and logs a warning through `logger.warn`.
