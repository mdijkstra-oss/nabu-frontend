# complaints-log

Register. Fielded entries, each with a complainant, a handler and dates.

- Person 🔴 — unstable. Three runs of the same input gave 3, 3 and 6 occurrences.
- Date 🟢 — four regions, one per entry plus the nested closure date at 10.

Known gaps:

- The rules do not say whether a `Handler:` field hands that person a passage. Runs disagree, which is what the instability is.
- The rules say an entry in a register reaches the whole entry, which makes the complainant own the handler's closure lines. Deliberate or not, it needs deciding.
