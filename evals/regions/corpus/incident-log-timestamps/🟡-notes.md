# incident-log-timestamps

Duty log. One date heading over eighteen timestamps, two named people.

- Person 🟢 — `okafor` twice (6–6, 15–16) and `bell` once. The duty officer's own actions correctly sit outside all three.
- Date 🟡 — all eighteen timestamps found, all resolved to the heading's day.

Known gaps:

- The heading owns only itself (0–0). Under containment it should own the whole log, with the eighteen timestamps nested inside.
- This is the file that outgrows a ten-occurrence stretch, so it is the one to watch when the cap becomes a char budget.
