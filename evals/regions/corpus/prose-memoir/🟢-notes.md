# prose-memoir

Prose. A first-person narrator the document never names, a brother and a mother described but never named, relative dates with nothing to count from.

- Person 🟢 — empty.
- Date 🟢 — empty.

All clean. This file is the over-firing detector: everything in it looks like it should produce something and none of it does. It caught a whole-document `brother` region before the rules were tightened.

Both gold sets are empty, so this file has no precision or recall. Score it as a count of false positives, pass at zero.
